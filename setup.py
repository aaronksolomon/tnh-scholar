from setuptools import setup, find_packages

setup(
    name='tnh-scholar',
    version='0.1',
    packages=find_packages(include=["tnh_process"]),
    install_requires=[],  # Add any dependencies here
)